#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.

bar 
{
    # font pango:MesloLGS Nerd Font Mono 
    # position bottom
    # separator_symbol "|"
    # # mode hide
    
    # pango_markup yes

    # # # When the status_command prints a new line to stdout, swaybar updates.
    # # # The default just shows the current date and time.
    # # status_command while true; do date +'%Y-%m-%d %I:%M:%S %p'; sleep 1; done
    # # # status_command while true; do echo "hi"; sleep 1; done

    # colors {
    #     statusline #ffffff
    #     background #323232
    #     inactive_workspace #32323200 #32323200 #5c5c5c
    # }
    
    swaybar_command waybar
}
